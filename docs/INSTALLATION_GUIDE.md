# 📖 دليل التثبيت والتشغيل - نظام الأرشيف v3.0

هذا الدليل يشرح خطوات تثبيت وتشغيل نظام الأرشيف v3.0 بالتفصيل.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر:
- حساب في [Supabase](https://supabase.com) (مجاني)
- حساب في [Railway](https://railway.app) (مجاني)
- بوت تليجرام من [@BotFather](https://t.me/BotFather)
- حساب Gmail (لإرسال رموز التحقق)

---

## 🔧 الخطوة 1: إعداد قاعدة البيانات

### 1.1 إنشاء مشروع Supabase

1. اذهب إلى [Supabase](https://supabase.com) وسجل دخول
2. اضغط **New Project**
3. أدخل اسم المشروع وكلمة مرور قوية لقاعدة البيانات
4. اختر المنطقة الأقرب لك
5. انتظر حتى يتم إنشاء المشروع (دقيقة واحدة تقريباً)

### 1.2 تشغيل سكربت قاعدة البيانات

1. من لوحة التحكم، اذهب إلى **SQL Editor**
2. افتح ملف `setup.sql` من المشروع
3. انسخ **كامل** محتوى الملف
4. الصقه في SQL Editor
5. اضغط **Run** أو **Ctrl+Enter**

**ملاحظة مهمة:** السكربت سيقوم بإنشاء:
- جدول المستخدمين (`users`)
- جدول الملفات (`files`)
- جدول الصلاحيات (`roles`)
- جدول رموز التحقق (`otp_codes`)
- حساب أدمن افتراضي:
  - البريد: `Moain.learn@gmail.com`
  - كلمة المرور: `DefaultPassword`
  - **يجب تغيير كلمة المرور بعد أول تسجيل دخول!**

### 1.3 الحصول على بيانات الاتصال

1. اذهب إلى **Settings** ← **API**
2. احفظ القيمتين التاليتين:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: مفتاح طويل يبدأ بـ `eyJ...`

---

## 🤖 الخطوة 2: إعداد بوت تليجرام

### 2.1 إنشاء البوت

1. افتح تليجرام وابحث عن [@BotFather](https://t.me/BotFather)
2. أرسل `/newbot`
3. أدخل اسم البوت (مثل: `My Archive Bot`)
4. أدخل username للبوت (يجب أن ينتهي بـ `bot`)
5. احفظ **Token** البوت (مثل: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 إنشاء مجموعة الأرشيف

1. أنشئ مجموعة خاصة جديدة في تليجرام
2. أضف البوت إلى المجموعة
3. اجعل البوت **Admin** (مسؤول) في المجموعة:
   - اضغط على اسم المجموعة
   - اضغط **Edit**
   - اضغط **Administrators**
   - اضغط **Add Administrator**
   - اختر البوت
   - فعّل جميع الصلاحيات

### 2.3 الحصول على معرف المجموعة

1. أضف [@userinfobot](https://t.me/userinfobot) إلى المجموعة مؤقتاً
2. سيرسل البوت معلومات المجموعة
3. احفظ **Chat ID** (سيكون رقماً سالباً يبدأ بـ `-100`)
4. يمكنك الآن حذف `@userinfobot` من المجموعة

---

## 📧 الخطوة 3: إعداد البريد الإلكتروني

### 3.1 إنشاء App Password في Gmail

1. اذهب إلى [https://myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
2. قد يطلب منك تسجيل الدخول مرة أخرى
3. في حقل "Select app"، اختر **Mail**
4. في حقل "Select device"، اختر **Other** واكتب `Archive Bot`
5. اضغط **Generate**
6. احفظ كلمة المرور المكونة من 16 حرف (مثل: `abcd efgh ijkl mnop`)

**ملاحظة:** إذا لم تظهر لك خيار App Passwords:
- تأكد من تفعيل التحقق بخطوتين (2FA) على حسابك
- قد تحتاج لتفعيل "Less secure app access" (غير محبذ)

---

## ☁️ الخطوة 4: النشر على Railway

### 4.1 تحضير المشروع

1. قم بعمل **Fork** لهذا المستودع إلى حسابك في GitHub
2. أو ارفع المشروع إلى مستودع خاص بك

### 4.2 إنشاء مشروع Railway

1. اذهب إلى [Railway](https://railway.app)
2. سجل دخول باستخدام حساب GitHub
3. اضغط **New Project**
4. اختر **Deploy from GitHub repo**
5. اختر المستودع الذي قمت بعمل Fork له

### 4.3 إضافة المتغيرات البيئية

1. بعد إنشاء المشروع، اضغط على **Variables**
2. أضف المتغيرات التالية:

```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TARGET_GROUP_ID=-1001234567890
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJ...
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=abcd efgh ijkl mnop
```

**استبدل القيم بالقيم الحقيقية التي حصلت عليها!**

### 4.4 النشر

1. Railway سيكتشف `Procfile` تلقائياً
2. سيتم إنشاء خدمتين:
   - **web**: الخادم (server.py)
   - **worker**: البوت (bot.py)
3. انتظر حتى يكتمل النشر (دقيقة تقريباً)
4. اضغط على **web** ثم **Settings** ← **Generate Domain** للحصول على رابط الموقع

---

## ✅ الخطوة 5: التحقق من التشغيل

### 5.1 التحقق من البوت

1. أرسل أي ملف إلى مجموعة الأرشيف
2. يجب أن يتم حفظ الملف تلقائياً
3. تحقق من Logs في Railway للتأكد من عدم وجود أخطاء

### 5.2 تسجيل الدخول كأدمن

1. افتح رابط الموقع الذي حصلت عليه من Railway
2. سجل دخول بالبيانات الافتراضية:
   - البريد: `Moain.learn@gmail.com`
   - كلمة المرور: `DefaultPassword`
3. **مهم جداً:** غيّر كلمة المرور فوراً!

### 5.3 إنشاء مستخدم جديد

1. من لوحة الأدمن، اضغط **إضافة مستخدم**
2. أدخل:
   - الرقم التعريفي (مثل: `USER001`)
   - الاسم الثلاثي (مثل: `محمد أحمد علي`)
3. الآن المستخدم يمكنه التسجيل:
   - يدخل الرقم التعريفي والاسم
   - يدخل بريده الإلكتروني
   - يستلم كود OTP
   - يدخل الكود ويضع كلمة مرور
   - يتم تفعيل حسابه!

---

## 🎯 الخطوة 6: إسناد الصلاحيات

1. من لوحة الأدمن، اذهب إلى **إدارة المستخدمين**
2. اختر مستخدماً
3. أسند له صلاحية من الصلاحيات الافتراضية:
   - **admin**: صلاحيات كاملة
   - **editor**: رفع وتعديل وحذف
   - **uploader**: رفع وتعديل فقط
   - **viewer**: مشاهدة فقط
4. أو أنشئ صلاحية مخصصة!

---

## 🐛 حل المشاكل الشائعة

### البوت لا يحفظ الملفات

- تأكد من أن البوت **Admin** في المجموعة
- تحقق من `TARGET_GROUP_ID` في المتغيرات البيئية
- راجع Logs في Railway

### لا يصل كود التحقق للبريد

- تأكد من صحة `SMTP_EMAIL` و `SMTP_PASSWORD`
- تحقق من أنك استخدمت **App Password** وليس كلمة مرور الحساب
- راجع مجلد Spam في البريد

### الموقع لا يفتح

- تأكد من أن خدمة **web** تعمل في Railway
- تحقق من أنك أنشأت Domain للمشروع
- راجع Logs للتأكد من عدم وجود أخطاء

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع Logs في Railway
2. تأكد من صحة جميع المتغيرات البيئية
3. افتح Issue في GitHub مع تفاصيل المشكلة

---

**🎉 تهانينا! نظام الأرشيف الخاص بك جاهز للاستخدام!**
