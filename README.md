# 📂 نظام أرشفة ملفات تليجرام - MVP

نظام بسيط وفعال لأرشفة الملفات باستخدام تليجرام كمساحة تخزين مجانية مع واجهة ويب لرفع وعرض الملفات.

## ✨ المميزات

- 🤖 **بوت تليجرام**: يستمع تلقائياً للملفات المرسلة في المجموعة ويحفظ روابطها
- 🌐 **واجهة ويب**: رفع وعرض الملفات من خلال موقع احترافي
- 📤 **رفع مباشر**: رفع الملفات من الموقع إلى تليجرام
- 🔍 **بحث وفلترة**: بحث سريع وفلترة حسب نوع الملف
- 📊 **إحصائيات**: عرض إحصائيات شاملة عن الملفات
- 💾 **قاعدة بيانات**: حفظ البيانات في Supabase
- ☁️ **استضافة سحابية**: جاهز للنشر على Railway

## 🛠️ التقنيات المستخدمة

- **Backend**: Python 3.11 + Flask
- **Bot**: python-telegram-bot
- **Database**: Supabase (PostgreSQL)
- **Frontend**: HTML5 + CSS3 + Vanilla JavaScript
- **Hosting**: Railway

## 📋 المتطلبات

1. **Telegram Bot Token** - احصل عليه من [@BotFather](https://t.me/BotFather)
2. **Target Group ID** - معرف المجموعة التي سيستمع لها البوت
3. **Supabase Account** - [سجل مجاناً](https://supabase.com)
4. **Railway Account** - [سجل مجاناً](https://railway.app) (للاستضافة)

## 🚀 التثبيت والإعداد

### 1️⃣ إعداد Telegram Bot

1. افتح [@BotFather](https://t.me/BotFather) في تليجرام
2. أرسل `/newbot` واتبع التعليمات
3. احفظ **Bot Token** الذي سيظهر لك
4. أنشئ مجموعة (Group) أو قناة (Channel) خاصة
5. أضف البوت إلى المجموعة كمسؤول (Admin)
6. احصل على **Group ID** باستخدام [@userinfobot](https://t.me/userinfobot)

### 2️⃣ إعداد Supabase

1. سجل في [Supabase](https://supabase.com) وأنشئ مشروعاً جديداً
2. اذهب إلى **SQL Editor**
3. انسخ محتوى ملف `setup.sql` وشغّله
4. اذهب إلى **Settings** → **API**
5. احفظ:
   - **Project URL**
   - **Anon/Public Key**

### 3️⃣ تثبيت المشروع محلياً

```bash
# استنساخ المشروع
git clone https://github.com/MoainAlabbasi/telegram-file-uploader.git
cd telegram-file-uploader

# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # في Windows: venv\Scripts\activate

# تثبيت المكتبات
pip install -r requirements.txt

# إنشاء ملف .env
cp .env.example .env
```

### 4️⃣ تكوين المتغيرات البيئية

افتح ملف `.env` وأضف بياناتك:

```env
BOT_TOKEN=8526337520:AAEIWegHcbKfnIt3f9UtPCVMGrGrpma4DV8
TARGET_GROUP_ID=-1002469448517
SUPABASE_URL=https://gmtcbemfxirorrsznlcr.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here
PORT=8080
```

### 5️⃣ تشغيل المشروع

**تشغيل الخادم (الموقع):**
```bash
python server.py
```

**تشغيل البوت (في نافذة منفصلة):**
```bash
python bot.py
```

افتح المتصفح على: `http://localhost:8080`

## 🌐 النشر على Railway

### الطريقة الأولى: من خلال GitHub (موصى بها)

1. ارفع المشروع على GitHub
2. سجل في [Railway](https://railway.app)
3. اضغط **New Project** → **Deploy from GitHub repo**
4. اختر المستودع `telegram-file-uploader`
5. أضف المتغيرات البيئية في **Variables**:
   ```
   BOT_TOKEN=your_bot_token
   TARGET_GROUP_ID=your_group_id
   SUPABASE_URL=your_supabase_url
   SUPABASE_KEY=your_supabase_key
   PORT=8080
   ```
6. Railway سيكتشف `Procfile` تلقائياً وينشئ خدمتين:
   - **web**: الموقع (server.py)
   - **worker**: البوت (bot.py)

### الطريقة الثانية: من خلال Railway CLI

```bash
# تثبيت Railway CLI
npm install -g @railway/cli

# تسجيل الدخول
railway login

# ربط المشروع
railway link

# نشر المشروع
railway up

# إضافة المتغيرات البيئية
railway variables set BOT_TOKEN=your_bot_token
railway variables set TARGET_GROUP_ID=your_group_id
railway variables set SUPABASE_URL=your_supabase_url
railway variables set SUPABASE_KEY=your_supabase_key
```

### ⚠️ ملاحظات مهمة للنشر

1. **Procfile** يحتوي على سطرين:
   - `web: python server.py` - للموقع
   - `worker: python bot.py` - للبوت (يعمل 24/7)

2. **Worker vs Web Service**:
   - البوت يجب أن يعمل كـ **Worker** وليس Web Service
   - Worker يظل يعمل في الخلفية دون توقف
   - Web Service قد يتوقف إذا لم يستقبل زيارات

3. **المتغيرات البيئية**:
   - يجب إضافة جميع المتغيرات في Railway
   - لا ترفع ملف `.env` إلى GitHub (محمي بـ `.gitignore`)

## 📖 كيفية الاستخدام

### رفع الملفات

**من الموقع:**
1. افتح الموقع
2. اسحب الملف إلى منطقة الرفع أو اضغط للاختيار
3. اضغط "رفع الملف"
4. سيتم رفع الملف إلى تليجرام وحفظ رابطه في قاعدة البيانات

**من تليجرام:**
1. أرسل أي ملف إلى المجموعة المستهدفة
2. البوت سيلتقطه تلقائياً ويحفظ رابطه في قاعدة البيانات

### عرض الملفات

1. افتح الموقع
2. استخدم البحث للعثور على ملف معين
3. استخدم الفلاتر لعرض نوع معين (صور، فيديو، صوت، مستندات)
4. اضغط "عرض" لفتح الملف في المتصفح
5. اضغط "تحميل" لتحميل الملف إلى جهازك

## 📁 هيكل المشروع

```
telegram-archive-mvp/
├── bot.py              # بوت تليجرام (يستمع للمجموعة)
├── server.py           # خادم Flask (الموقع + API)
├── index.html          # واجهة الويب
├── setup.sql           # إعداد قاعدة البيانات
├── requirements.txt    # المكتبات المطلوبة
├── Procfile           # إعدادات Railway
├── runtime.txt        # إصدار Python
├── .env.example       # مثال للمتغيرات البيئية
├── .gitignore         # ملفات Git المستبعدة
└── README.md          # هذا الملف
```

## 🔧 API Endpoints

### رفع ملف
```http
POST /upload
Content-Type: multipart/form-data

Body:
- file: File (required)
```

### الحصول على جميع الملفات
```http
GET /api/files
```

### الإحصائيات
```http
GET /api/stats
```

### فحص الصحة
```http
GET /health
```

## 🗄️ قاعدة البيانات

### جدول `files`

| العمود | النوع | الوصف |
|--------|------|-------|
| `id` | SERIAL | المعرف الفريد |
| `file_name` | TEXT | اسم الملف |
| `file_size` | BIGINT | حجم الملف بالبايت |
| `file_type` | TEXT | نوع الملف (image/video/audio/document) |
| `mime_type` | TEXT | MIME type |
| `telegram_file_id` | TEXT | معرف الملف في تليجرام |
| `file_url` | TEXT | رابط الملف في تليجرام |
| `message_id` | INTEGER | معرف الرسالة في تليجرام |
| `created_at` | TIMESTAMP | تاريخ الرفع |

## 🔐 الأمان

- ✅ **Row Level Security (RLS)** مفعّل في Supabase
- ✅ **Environment Variables** للمعلومات الحساسة
- ✅ **HTTPS** في الإنتاج (Railway)
- ⚠️ **ملاحظة**: هذا مشروع MVP للاستخدام الشخصي

## 🐛 حل المشاكل الشائعة

### البوت لا يستجيب للملفات

**الحل:**
- تأكد من صحة `BOT_TOKEN`
- تأكد من صحة `TARGET_GROUP_ID`
- تأكد أن البوت مسؤول (Admin) في المجموعة
- تحقق من سجلات البوت (logs)

### خطأ في رفع الملف من الموقع

**الحل:**
- تأكد من أن حجم الملف أقل من 50 MB
- تأكد من أن الخادم يعمل (`python server.py`)
- تحقق من سجلات الخادم

### الملفات لا تظهر في الموقع

**الحل:**
- تأكد من تشغيل `setup.sql` في Supabase
- تأكد من صحة `SUPABASE_URL` و `SUPABASE_KEY`
- افتح Console في المتصفح وتحقق من الأخطاء

## 📝 التحديثات المستقبلية

- [ ] نظام مصادقة (Authentication)
- [ ] رفع ملفات متعددة
- [ ] معاينة الصور والفيديوهات
- [ ] تنظيم الملفات في مجلدات
- [ ] مشاركة الملفات عبر روابط

## 🤝 المساهمة

المساهمات مرحب بها! إذا كان لديك اقتراح:

1. Fork المشروع
2. أنشئ فرعاً للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت **MIT License**.

## 👨‍💻 المطور

**MoTech Team**

- GitHub: [@MoainAlabbasi](https://github.com/MoainAlabbasi)
- المشروع: [telegram-file-uploader](https://github.com/MoainAlabbasi/telegram-file-uploader)

---

<div align="center">

**صُنع بـ ❤️ بواسطة MoTech Team**

⭐ إذا أعجبك المشروع، لا تنسَ إعطائه نجمة!

</div>
